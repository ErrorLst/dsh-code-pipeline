name: code agent
description: 基于 PTC 模式（Code Mode SDK）的子代理流水线：subagent_plan（规划，只读）/ subagent_impl（实现）/ subagent_review（评审，只读）三个派发工具（后台派发、每阶段独立 provider/model/思考等级/并发上限、派发消息整体落单个临时文件由子代理 read 读取），可独立或任意组合（仅规划 / 仅实现 / 仅评审+主代理合并 / 完整 plan→人工门→impl↔review），独立目标优先并行派发以加快进度；已屏蔽通用 subagent 派发工具。
